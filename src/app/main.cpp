#include <iostream>

#include "myproject/core/core.hpp"

int main() {
    std::cout << "Hello, World!\n" << std::flush;
    std::cout << "Add(1, 2) = " << Add(1, 2) << "\n" << std::flush;
    return 0;
}
